<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>

    <div class="container">
        <h1>To Do List</h1>
        <h2>Name: Ishan Sandaruwan</h2>
        
        <div class="to-dos">
            <% if(locals.todos){ %>
                <% todos.forEach((todo, index) => { %>
                    <div class="to-do">
                      <input type="checkbox" name="todoCheckbox">
                      <p><%= todo %></p>
                    </div>
                  <% }); %>
            <% } %>
            <form action="/submit" method="post">
                <input type="text" name="todo" required placeholder="new item">
                <button type="submit">+</button>
            </form>
            
        </div>
        <div class="footer">
            <div class="date">
                <%= day  %>,<%= month  %> <%= date  %> <%= year  %>
            </div>
        </div>
    </div>

</body>
</html>